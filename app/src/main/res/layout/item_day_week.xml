<?xml version="1.0" encoding="utf-8"?>
<!--
    item_day_week.xml
    Cada celda del calendario semanal en la pantalla Inicio.
    Se infla programáticamente desde InicioFragment.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_weight="1"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="4dp">

    <!-- Letra del día: L, M, M, J, V, S, D -->
    <TextView
        android:id="@+id/tv_day_letter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/poppins_regular"
        android:text="L"
        android:textColor="@color/text_soft"
        android:textSize="11sp" />

    <!-- Círculo con número -->
    <TextView
        android:id="@+id/tv_day_number"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_marginTop="4dp"
        android:background="@drawable/bg_day_circle_default"
        android:fontFamily="@font/poppins_semibold"
        android:gravity="center"
        android:text="1"
        android:textColor="@color/text_dark"
        android:textSize="14sp" />

</LinearLayout>